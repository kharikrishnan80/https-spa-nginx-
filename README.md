# https-spa-nginx-
# HTTPS SPA on Nginx (Self-signed) — Docker Compose

## Prereqs (Ubuntu)
- Docker + Docker Compose plugin

Install:
```bash
sudo apt update
sudo apt install -y docker.io docker-compose-plugin
sudo usermod -aG docker $USER
newgrp docker
